dev.dir = dev
deploy.dir = dist
src.dir = ${dev.dir}/src
lib.dir = ${dev.dir}/lib
build.dir = ${dev.dir}/build
dist.dir = ${deploy.dir}

yuicompressor.jar = ${build.dir}/yuicompressor/yuicompressor-2.4.2.jar

product.name = PerspectiveTransform
version.number = 0.1.0

dist.name = ${product.name}.js
dist.min.name = ${product.name}.min.js
